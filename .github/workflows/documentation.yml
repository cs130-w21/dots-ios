name: PublishDocumentation

on:
  push:
    branches: 
      - master

jobs:
  deploy_docs:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v1
    - name: Publish Jazzy Docs
      uses: steven0351/publish-jazzy-docs@v1
      with:
        personal_access_token: d1abb0e7ab4e776cc3e7ee3d70deae5172f35b72
        config: .jazzy.yml
